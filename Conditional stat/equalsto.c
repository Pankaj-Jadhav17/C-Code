#include<stdio.h>
int main()
{
    int a=0,b=0;
    printf("enter two number...");
    scanf("%d%d",&a,&b);
    if(a>b)
    {
        printf("number is greater");
    }
     else if(b>a)
     
            printf("%d is greater ");
            else
            printf("%d is equal");
}